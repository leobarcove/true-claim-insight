version: '3.8'

services:
  # ============ DATABASE ============
  postgres:
    image: postgres:16-alpine
    container_name: tci-postgres
    environment:
      POSTGRES_USER: tci
      POSTGRES_PASSWORD: localdev
      POSTGRES_DB: true_claim_insight
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tci"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============ CACHE ============
  redis:
    image: redis:7.4-alpine
    container_name: tci-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============ AWS LOCAL ============
  localstack:
    image: localstack/localstack:latest
    container_name: tci-localstack
    environment:
      SERVICES: s3,secretsmanager
      DEFAULT_REGION: ap-southeast-5
      AWS_ACCESS_KEY_ID: localstack
      AWS_SECRET_ACCESS_KEY: localstack
    ports:
      - "4566:4566"
    volumes:
      - localstack_data:/var/lib/localstack
      - ./scripts/localstack-init.sh:/etc/localstack/init/ready.d/init.sh

  # ============ MAIL (DEV) ============
  mailhog:
    image: mailhog/mailhog:latest
    container_name: tci-mailhog
    ports:
      - "1025:1025"  # SMTP
      - "8025:8025"  # Web UI

volumes:
  postgres_data:
  redis_data:
  localstack_data:
